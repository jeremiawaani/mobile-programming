import {StyleSheet, Text, View, TouchableOpacity} from 'react-native';
import React from 'react';

const CenterModal = () => {
  return (
    <View>
      <Text>CenterModal</Text>
    </View>
  );
};

export default CenterModal;

const styles = StyleSheet.create({});
